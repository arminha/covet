[package]
name = "covet"
version = "0.5.2"
authors = ["Armin Häberling <armin.aha@gmail.com>"]
build = "build.rs"

description = "A CLI and web frontend for scanning with HP Envy printer / scanners"
homepage = "https://github.com/arminha/covet"
repository = "https://github.com/arminha/covet"

readme = "README.md"
categories = ["command-line-utilities"]
license = "GPL-3.0"

include = [
  "Cargo.toml",
  "Cargo.lock",
  "src/**/*",
  "build.rs",
  "README.md",
  "COPYRIGHT",
  "LICENSE"
]

[badges]
travis-ci = { repository = "arminha/covet", branch = "master" }
maintenance = { status = "passively-maintained" }

[profile.release]
lto = true

[build-dependencies]
clap = "2.29"

[dependencies]
hyper = "0.10"
hyper-native-tls = "0.2"
xmltree = "0.7"
xml-rs = "0.7"
time = "0.1"
# cli
clap = "2.29"
# web
iron = "0.6"
router = "0.6"
urlencoded = "0.6"
sha2 = "0.7"
base64 = "0.6"
