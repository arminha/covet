# defaults for covet server

# pulled in from the init script; makes things easier.
NAME=covet

PIDFILE=/var/run/$NAME/$NAME.pid

# user and group to be invoked as (default to web-video-dl)
COVET_USER=$NAME
COVET_GROUP=$NAME

# log location.  this may be a syslog facility.priority
COVET_LOG=/var/log/$NAME/$NAME.log

# port for HTTP connector (default 8070)
HTTP_PORT=8070

# hostname or ip of the scanner
SCANNER=change_scanner_hostname
