{
  "name": "covet-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot index.ts",
    "build": "bun run build.ts",
    "lint": "bun run biome lint",
    "format": "bun run biome format --fix",
    "check": "bun run biome check"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "@types/bun": "1.3.6"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
